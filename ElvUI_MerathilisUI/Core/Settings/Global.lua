local MER, F, E, L, V, P, G = unpack(select(2, ...))

G.core = {
	loginMsg = true,
	logLevel = 2,
	compatibilityCheck = true,
	fixLFG = true,
	fixCVAR = true,
}

G.mail = {
	contacts = {
		alts = {},
		favorites = {},
	}
}

G.bags = {
	CustomJunkList = {}
}
